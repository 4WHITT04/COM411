#start of function with multiple parameters.
def climb_ladder(stepsleft, stepsclimbed):

  #start of if else statement nested in function.
  if (stepsleft > stepsclimbed):
    
    print("Still a way to go, man this ladder is long!")
  
  else:

    print("I can see the top Beep!")


#function calls with multiple parameters.
climb_ladder(5,2)
climb_ladder(2,5)
